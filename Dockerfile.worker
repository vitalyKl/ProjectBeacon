FROM node:22-bookworm-slim

WORKDIR /app

RUN corepack enable && corepack prepare pnpm@10.34.5 --activate

COPY . .

RUN pnpm install --frozen-lockfile

ENV NODE_ENV=production

CMD ["pnpm", "--filter", "@beacon/worker", "start"]
