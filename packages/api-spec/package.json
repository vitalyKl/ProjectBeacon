{
  "name": "@beacon/api-spec",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run",
    "generate:openapi": "vitest run src/openapi.test.ts"
  },
  "dependencies": {
    "@beacon/shared": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@beacon/config": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "^3.2.4"
  }
}
