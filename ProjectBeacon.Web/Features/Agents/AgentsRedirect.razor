@page "/agents"
@layout MainLayout
@attribute [Authorize]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/settings", replace: true);
}
